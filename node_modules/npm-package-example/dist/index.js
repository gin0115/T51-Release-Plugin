"use strict";

function sumHelper(a, b) {
    return a + b;
}

module.exports = {
    sum: function sum() {
        return Array.prototype.slice.call(arguments).reduce(sumHelper, 0);
    },
    sumES6: function sumES6() {
        return Array.from(arguments).reduce(sumHelper, 0);
    }
};